# Project File Structure Documentation

See the root `README.md` or prompt specifications for details regarding the layout of Rust core modules (`src-tauri/`), React UI modules (`src/`), PDF templates (`templates/`), and export target paths (`exports/`).
